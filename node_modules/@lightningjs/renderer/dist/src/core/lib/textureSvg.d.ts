import { type TextureData } from '../textures/Texture.js';
/**
 * Tests if the given location is a SVG
 * @param url
 * @remarks
 * This function is used to determine if the given image url is a SVG
 * image
 * @returns
 */
export declare function isSvgImage(url: string): boolean;
/**
 * Loads a SVG image
 * @param url
 * @returns
 */
export declare const loadSvg: (url: string, width: number | null, height: number | null, sx: number | null, sy: number | null, sw: number | null, sh: number | null) => Promise<TextureData>;
